type Query {
    listings: [Listing]
}

type Mutation {
    createListing(input: CreateListingInput!): Listing
}

type Listing {
    id: Int!
    name: String!
}

input CreateListingInput {
    name: String!
}
